# Omni-Dev Agent

## Virtual Environment
For dependency management, it's recommended to use a virtual environment.

```bash
python -m venv venv
source venv/bin/activate  # On Windows use `venv\Scripts\activate`
```

## Dependencies
List all dependencies here.

```
Flask
SQLAlchemy
requests
pytest
selenium
sphinx
```

Install dependencies with:

```bash
pip install -r requirements.txt
```

